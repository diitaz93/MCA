#include <stdio.h>

int main(void){
  float a=0.02;
  float b=20.0;
  double c=0.02;
  double d=20.0;

  printf("a (single): %.20f\n", a);
  printf("b (single): %.20f\n", b);
  printf("c (double): %.20f\n", c);
  printf("d (double): %.20f\n", d);

  return 0;
}
